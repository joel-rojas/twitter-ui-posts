import { BodyListenerDirective } from './body-listener.directive';

describe('BodyListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyListenerDirective();
    expect(directive).toBeTruthy();
  });
});
